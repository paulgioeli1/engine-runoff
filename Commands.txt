npx degit phaserjs/template-vite-ts phaser
cd phaser
npm install
npm run dev
This works when we're running a vite server in the project. If we're not, we can just run the following:
python -m http.server 8080
